import { Box, Container, Heading } from "theme-ui";

export const Footer: React.FC = () => (
  <Box sx={{ background: "secondary", color: "background", minHeight: "5vh" }}>
    <Container>
      <Heading>Footer</Heading>
    </Container>
  </Box>
);
